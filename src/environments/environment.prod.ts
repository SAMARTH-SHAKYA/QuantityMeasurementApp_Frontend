export const environment = {
  production: true,
  // We will update this later once your backend is on Render. 
  // For now, we use a placeholder so the build passes and URL is parameterized on Vercel.
  apiUrl: 'https://quantity-measurement-api-placeholder.onrender.com/api'
};
